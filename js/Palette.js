export default class Palette {
    constructor(id, label, background, metric, groups) {
        this.id = id;
        this.label = label;
        this.background = background;
        this.metric = metric;
        this.groups = groups;
    }
}
